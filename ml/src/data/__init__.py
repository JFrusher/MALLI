"""Data loading and preprocessing utilities for the M.A.L.L.I. project.

This package contains dataset loader implementations and helpers used by the
training pipeline. The top-level modules under ``ml/src/data/loaders/``
implement concrete dataset classes and should be imported directly when
needed (e.g. ``from ml.src.data.loaders.nih_loader import MalariaDataset``).
"""

